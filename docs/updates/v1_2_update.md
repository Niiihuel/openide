---
{
  "badge": "What's new",
  "title": "OpenIDE 1.2.0",
  "features": [
    { "icon": "$(mic)", "title": "Dictation that fits the model", "description": "Voice picks the audio format each provider and model supports, with a microphone test and cancellation." },
    { "icon": "$(layout)", "title": "Editor foundation 1.136.1", "description": "Code OSS 1.136.1 on Electron 42, with the same extension API as current VS Code." },
    { "icon": "$(diff)", "title": "Clearer changes", "description": "Full-width diff highlights, a changed-files tray and keep/undo per file or block." },
    { "icon": "$(sync)", "title": "Models that refresh in place", "description": "Codex reads the account's catalog and refreshes without restarting; removing an account falls back to a saved one." },
    { "icon": "$(shield)", "title": "Verified updates", "description": "Stable feeds are published only after release assets are public and their signatures and hashes were checked." }
  ],
  "buttons": [
    { "label": "Release notes", "commandId": "update.showCurrentReleaseNotes", "style": "primary" }
  ]
}
---
OpenIDE 1.2.0 updates the editor foundation to Code OSS 1.136.1, adds provider-aware voice dictation, improves change review in the chat, and verifies release assets before advancing the update feed.
