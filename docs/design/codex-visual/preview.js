/* Copyright OpenIDE. Visual approval prototype, independent of the IDE runtime. */
'use strict';
const paths = {
 queue:'M4 4v13h13 M8 6h10 M8 10h7 m1 3 5 4-5 4', trash:'M4 6h16 M9 6V3h6v3 M6 6l1 15h10l1-15 M10 10v7M14 10v7',
 panel:'M4 3h16v18H4z M9 3v18', search:'M16 16l5 5 M18 10a8 8 0 1 1-16 0 8 8 0 0 1 16 0',
 back:'M20 12H4m6-6-6 6 6 6', arrow:'M12 20V4m-7 7 7-7 7 7', chevron:'m9 5 7 7-7 7', down:'m6 9 6 6 6-6',
 folder:'M3 6h7l2 3h9v11H3z', file:'M5 2h9l5 5v15H5z M14 2v6h5 M8 12h8 M8 16h6',
 plus:'M12 4v16M4 12h16', close:'m6 6 12 12M6 18 18 6', check:'m4 12 5 5L20 5',
 settings:'M9 3h6l1 4 4 2v6l-4 2-1 4H9l-1-4-4-2V9l4-2z M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0',
 clock:'M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0 M12 6v6l4 2', chat:'M3 4h18v14h-8l-5 4v-4H3z',
 edit:'m15 3 6 6-12 12H3v-6z M12 6l6 6', git:'M6 6v12 M18 6v5c0 3-12 1-12 4 M9 3a3 3 0 1 1-6 0 3 3 0 0 1 6 0 M21 3a3 3 0 1 1-6 0 3 3 0 0 1 6 0',
 globe:'M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0 M3 12h18 M12 3c-6 5-6 13 0 18 6-5 6-13 0-18',
 shield:'m12 2 9 4v7c0 5-9 9-9 9s-9-4-9-9V6z m-5 8 3 3-3 3 M12 15h4',
 hand:'M8 12V5a2 2 0 0 1 4 0v7-8a2 2 0 0 1 4 0v8-6a2 2 0 0 1 4 0v9c0 9-10 10-13 5l-5-7a2 2 0 0 1 3-2l3 3',
 goal:'M20 10a9 9 0 1 1-6-7 M16 11a4 4 0 1 1-4-3 M12 12l9-9 M17 3h4v4',
 bulb:'M8 17c0-3-4-4-4-8a8 8 0 0 1 16 0c0 4-4 5-4 8z M9 21h6',
 pause:'M8 5v14M16 5v14', play:'m7 4 14 8-14 8z', terminal:'M3 4h18v16H3z m4 4 4 4-4 4 M13 16h4',
 copy:'M8 8h13v13H8z M16 8V3H3v13h5', refresh:'M20 8V3l-4 4 M20 8a9 9 0 1 0 1 7',
 bolt:'m14 2-10 12h7l-1 8L21 9h-8z', attach:'m8 12 7-7a4 4 0 0 1 6 6L10 22a6 6 0 0 1-8-8L13 3 M6 16l9-9',
 mic:'M9 4a3 3 0 0 1 6 0v8a3 3 0 0 1-6 0z M5 10v2a7 7 0 0 0 14 0v-2 M12 19v3',
 sun:'M16 12a4 4 0 1 1-8 0 4 4 0 0 1 8 0 M12 1v2M12 21v2M1 12h2M21 12h2M4 4l2 2M18 18l2 2M4 20l2-2M18 6l2-2',
 user:'M16 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0 M4 22v-4c0-7 16-7 16 0v4',
 download:'M12 2v13m-5-5 5 5 5-5 M3 16v5h18v-5', more:'M4 12h1M11 12h1M18 12h1',
 external:'M14 3h7v7 M21 3 10 14 M10 3H3v18h18v-7', pin:'m8 3 10 0-2 6 4 4H4l4-4z M12 13v9',
 archive:'M3 3h18v5H3z M5 8v13h14V8 M9 12h6', cube:'m12 2 10 5v10l-10 5-10-5V7z M2 7l10 5 10-5 M12 12v10'
};
document.body.insertAdjacentHTML('afterbegin', `<svg aria-hidden="true" style="position:absolute;width:0;height:0;overflow:hidden"><defs>${Object.entries(paths).map(([id,d])=>`<symbol id="${id}" viewBox="0 0 24 24"><path d="${d}"/></symbol>`).join('')}</defs></svg>`);
const $=s=>document.querySelector(s);
const icon=n=>`<svg aria-hidden="true"><use href="#${n}"/></svg>`;
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const ib=(n,label,action)=>`<button class="icon-button" aria-label="${label}" data-tip="${label}" data-action="${action}">${icon(n)}</button>`;
const pop=(label,name,cls='btn outline')=>`<button class="${cls}" data-pop="${name}" aria-haspopup="${name==='effort'?'dialog':'menu'}" aria-expanded="false">${label}${icon('down')}</button>`;
const sampleDiffs=()=>[{file:'tokens.css',added:24,removed:8},{file:'tooltip.js',added:12,removed:4},{file:'selector-adapter.js',added:6,removed:0}];
const state={diffs:sampleDiffs(),reviewBusy:false,trayFilter:'all',trayOpen:null,queued:'Ajustá los bordes y verificá los popovers',terminalStopped:false,view:'settings',category:'General',permission:1,effort:2,model:'GPT-6 Astra',switches:{defaults:true},attachments:2,messages:[],paused:false,context:true,plan:false,tab:'app.ts',sidebar:true,draft:'',activityStep:0,demoRunning:false,activityOpen:false};
const effortNames=['Light','Medium','High','Extra High','Ultra'];
const modelSelector=()=>`<openide-model-selector model-name="${esc(state.model)}" value="${state.effort}"></openide-model-selector>`;
const switcher=(id,label,on=false)=>`<button class="switch" role="switch" aria-label="${label}" aria-checked="${state.switches[id]??on}" data-switch="${id}"><span></span></button>`;
const row=(title,desc,control)=>`<div class="setting" data-search="${esc((title+' '+desc).toLowerCase())}"><div class="setting-copy"><h3>${title}</h3><p>${desc}</p></div><div class="setting-action">${control}</div></div>`;
const section=(title,rows)=>`<section class="settings-section"><h2>${title}</h2><div class="settings-group">${rows}</div></section>`;
const categories=[['Personal',[['General','settings'],['Importar','download'],['Perfil','user'],['Apariencia','sun'],['Voz','mic'],['Configuración','shield'],['Personalización','bulb'],['Atajos de teclado','bolt'],['Uso y estadísticas','clock']]],['Integraciones',[['Herramientas','terminal'],['Plugins','cube'],['Navegador','globe']]],['Desarrollo',[['Hooks','bolt'],['Conexiones','globe'],['Git','git'],['Entornos','panel']]]];
function sidebar(){
 if(state.view==='settings') return `<div class="sidebar-top"><button class="nav-row" data-view="chat">${icon('back')}Volver a la app</button></div><label class="search">${icon('search')}<input id="settings-search" aria-label="Buscar ajustes" placeholder="Buscar ajustes…"></label><nav class="nav-scroll" aria-label="Secciones de ajustes">${categories.map(([group,items])=>`<div class="section-label">${group}</div>${items.map(([name,i])=>`<button class="nav-row ${state.category===name?'active':''}" data-category="${name}" ${state.category===name?'aria-current="page"':''}>${icon(i)}<span>${name}</span></button>`).join('')}`).join('')}</nav>`;
 return `<div class="sidebar-top"><strong>OpenIDE</strong>${icon('down')}<div class="end">${ib('search','Buscar','search')}${ib('settings','Abrir ajustes','settings')}</div></div><nav class="nav-scroll" aria-label="Navegación principal">${[['Nueva conversación','edit','new'],['Pull requests','git','demo'],['Programadas','clock','demo'],['Plugins','cube','demo']].map(([label,i,action])=>`<button class="nav-row" data-action="${action}">${icon(i)}${label}</button>`).join('')}<div class="section-label">Proyectos</div><button class="nav-row" data-view="editor">${icon('folder')}openide</button>${['Migración visual del IDE','Mejorar el sistema Canvas','Revisar herramientas'].map((label,i)=>`<button class="nav-row task ${i===0?'active':''}" data-action="task" data-tip="${label} · openide"><span>${label}</span><span class="row-actions" aria-hidden="true">${icon('pin')}${icon('archive')}</span></button>`).join('')}<button class="nav-row" data-action="demo">${icon('folder')}portafolio</button><div class="section-label">Revisión del diseño</div>${[['Ajustes','settings'],['Conversación','chat'],['Componentes','components'],['Editor','editor'],['Movimiento','motion'],['Bandejas','trays']].map(([label,v])=>`<button class="nav-row ${state.view===v?'active':''}" data-view="${v}">${icon(v==='trays'?'queue':v==='motion'?'bolt':v==='components'?'cube':v==='editor'?'file':v)}${label}</button>`).join('')}</nav><footer class="sidebar-footer"><span class="avatar">OI</span>OpenIDE${ib('settings','Abrir ajustes','settings')}</footer>`;
}
function settings(){
 let content;
 if(state.category==='General')content=section('Permisos',row('Permisos predeterminados','La IA puede leer y editar archivos del proyecto. Puede solicitar acceso adicional cuando lo necesite.',switcher('defaults','Permisos predeterminados',true))+row('Acceso completo','Permite actuar fuera del proyecto. Este control solo cambia el estado de la demostración.',switcher('full','Acceso completo')))+section('General',row('Carpeta de tareas sin proyecto','Ubicación donde se guardan las tareas creadas fuera de un proyecto.',`<span class="setting-value">~/Documentos/OpenIDE</span><button class="btn" data-action="folder">Cambiar</button>`)+row('Destino de archivos','Dónde se abren los archivos y las carpetas.',pop('OpenIDE','destination'))+row('Idioma','Idioma de la interfaz de la aplicación.',pop('Detección automática','language'))+row('Panel inferior','Mostrar el control del panel en la cabecera.',switcher('bottom','Panel inferior'))+row('Evitar suspensión','Mantener el equipo activo mientras se ejecuta una tarea.',switcher('sleep','Evitar suspensión'))+row('Velocidad','Ritmo de trabajo de las tareas y subagentes.',pop('Estándar','speed')));
 else if(state.category==='Apariencia')content=section('Interfaz',row('Tema','Una paleta neutra y superficies diferenciadas.',pop('Oscuro · referencia','theme'))+row('Densidad','Espacio de las filas de navegación y controles.',pop('Cómoda','density'))+row('Movimiento reducido','Reducir animaciones y transiciones.',switcher('motion','Movimiento reducido')))+section('Editor',row('Familia tipográfica','La tipografía del código es independiente de la interfaz.','<input class="field" aria-label="Tipografía del editor" value="Consolas" style="width:170px">')+row('Tamaño de fuente','Escala del texto del editor en píxeles.','<input class="field" type="number" min="10" max="32" value="13" aria-label="Tamaño de fuente" style="width:75px">'));
 else content=section(state.category,row(`Activar ${state.category.toLowerCase()}`,'Control de muestra para revisar jerarquía, espaciado y estados.',switcher('category-'+state.category,'Activar '+state.category,true))+row('Configuración del proyecto','Aplicar esta preferencia al proyecto actual.',pop('Proyecto actual','scope'))+row('Preferencias avanzadas','Revisar las opciones adicionales de esta sección.','<button class="btn" data-action="dialog">Configurar</button>'))+`<p class="test-note">Sección representativa del diseño. No modifica la configuración del IDE.</p>`;
 return `<div class="settings-scroll"><div class="settings-content"><h1>${state.category}</h1>${content}<p class="test-note">Prototipo local · los cambios de esta vista son temporales.</p><div id="search-empty" class="empty-state" hidden>No hay ajustes que coincidan.</div></div></div>`;
}
const activityTasks=['Buscando estilos en tokens.css','Leyendo preview.js','Leyendo motion.js','Revisando selector-adapter.js','Exploración completada'];
const activityReads=['preview.js','motion.js','selector-adapter.js'];
let activityTimer;
function activities(){return `<div class="activity-row">${icon('goal')}<strong>Unificar el diseño del IDE</strong><span class="badge">3/5</span>${ib('external','Ver informe del objetivo','goal')}</div><div class="progress"><span></span></div><div class="activity-row live-activity"><button class="activity-disclosure" aria-expanded="${state.activityOpen}" data-action="activity-details">${icon('search')}<span class="activity-label ${!state.paused&&state.activityStep<4?'shimmer':''}" data-live-label>${activityTasks[state.activityStep]}</span>${icon('down')}</button>${ib(state.paused?'play':'pause',state.paused?'Reanudar subagente':'Pausar subagente','pause')}</div><div class="activity-reveal ${state.activityOpen?'is-open':''}" ${state.activityOpen?'':'inert'}><div><div class="activity-log">${activityReads.map(file=>`<div class="activity-read">${icon('file')}<span>Leído <button class="activity-file" data-action="activity-file" data-file="${file}">${file}</button></span></div>`).join('')}<div class="activity-read">${icon('search')}<span>Buscado <code>focus-visible|popover|tooltip</code> en <button class="activity-file" data-action="activity-file" data-file="tokens.css">tokens.css</button></span></div></div></div></div><div class="activity-row">${icon('terminal')}Servidor de desarrollo <span class="badge">localhost:3000</span>${ib('terminal','Ver terminal en segundo plano','terminal')}</div><div class="motion-demo-controls"><button class="btn ghost" data-action="replay-activity">${icon('play')}Reproducir actividad</button><button class="btn ghost" data-action="next-activity">Siguiente tarea ${icon('chevron')}</button></div>`;}
function paintActivity(){
 document.querySelectorAll('[data-live-label]').forEach(el=>{OIMotion.swap(el,activityTasks[state.activityStep]);el.classList.toggle('shimmer',!state.paused&&state.activityStep<4);});
 document.querySelectorAll('[data-step]').forEach(el=>{const i=+el.dataset.step;el.classList.toggle('current',i===state.activityStep);el.classList.toggle('done',i<state.activityStep);el.querySelector('svg').outerHTML=icon(i<state.activityStep?'check':i===state.activityStep?'clock':'file');});
 document.querySelectorAll('[data-action="pause"]').forEach(n=>{n.innerHTML=icon(state.paused?'play':'pause');n.setAttribute('aria-label',state.paused?'Reanudar subagente':'Pausar subagente');n.dataset.tip=n.getAttribute('aria-label');});
}
function startActivity(){clearInterval(activityTimer);state.activityStep=0;state.paused=false;state.demoRunning=true;paintActivity();activityTimer=setInterval(()=>{if(state.paused||document.hidden)return;if(!document.querySelector('[data-live-label]')){clearInterval(activityTimer);state.demoRunning=false;return;}state.activityStep++;paintActivity();if(state.activityStep>=4){clearInterval(activityTimer);state.demoRunning=false;toast('Recorrido visual completado. Podés reproducirlo otra vez.');}},4200);}
function planPreview(){return `<section class="plan-preview"><header>${icon('file')}<span>visual-migration.plan.md</span><span class="badge">Plan</span></header><h3>Unificar los componentes del IDE</h3><p>Conservar la densidad de OpenIDE y compartir superficies, bordes y movimiento.</p><div class="plan-check">${icon('check')}Tokens y estados compartidos</div><div class="plan-check">${icon('check')}Compositor, menús y agentes</div><footer><button class="btn outline" data-action="dialog">Ver plan ${icon('external')}</button><div class="build-split"><button data-action="build-demo">Build <kbd>Ctrl ↵</kbd></button><button data-pop="build" aria-label="Opciones de Build" aria-haspopup="menu" aria-expanded="false">${icon('down')}</button></div></footer></section>`;}

function trayRow(id,label,leading,meta,actions,detail){const expanded=state.trayOpen===id;return `<section class="composer-tray" data-tray-kind="${id}"><div class="tray-summary"><button class="tray-disclosure" data-action="tray-toggle" data-tray="${id}" aria-expanded="${expanded}">${icon(leading)}<span class="tray-label">${label}</span>${meta?`<span class="tray-meta">${meta}</span>`:''}${icon('down')}</button><div class="tray-actions">${actions}<button class="icon-button" data-pop="tray-${id}" aria-label="Opciones de ${id==='queue'?'la cola':id==='terminal'?'terminal':'cambios'}" aria-haspopup="menu" aria-expanded="false">${icon('more')}</button></div></div><div class="tray-reveal ${expanded?'is-open':''}" ${expanded?'':'inert'}><div><div class="tray-detail oi-scroll-body">${detail}</div></div></div></section>`;}
function composerTrays(){
 const filter=state.trayFilter,items=[];
 if(state.queued&&['all','queue'].includes(filter))items.push(trayRow('queue',esc(state.queued),'queue','1 en cola',`<button class="tray-steer" data-action="tray-steer" aria-label="Enviar ahora el mensaje en cola">${icon('arrow')}<span>Enviar ahora</span></button><button class="icon-button tray-hover-action" data-action="tray-remove" aria-label="Quitar mensaje de la cola">${icon('trash')}</button>`,`<p>${esc(state.queued)}</p><small>Se enviará después de la respuesta actual. Mensaje de ejemplo.</small>`));
 if(['all','terminal'].includes(filter))items.push(trayRow('terminal','npm run dev','terminal',state.terminalStopped?'Finalizada':'En ejecución',`<button class="icon-button tray-hover-action" data-action="tray-stop" aria-label="${state.terminalStopped?'Reiniciar':'Detener'} terminal de ejemplo">${icon(state.terminalStopped?'play':'pause')}</button>`,`<pre>$ npm run dev\n✓ Servidor listo en http://localhost:3000\n${state.terminalStopped?'Proceso finalizado.':'Esperando cambios…'}</pre><small>Salida ilustrativa · no ejecuta comandos.</small>`));
 if(state.diffs.length&&['all','diff'].includes(filter)){
  const added=state.diffs.reduce((sum,f)=>sum+f.added,0),removed=state.diffs.reduce((sum,f)=>sum+f.removed,0);
  const bulk=state.reviewBusy?`<button class="tray-steer" data-action="tray-review-stop" aria-label="Detener agente de ejemplo antes de revisar">${icon('pause')}Detener</button>`:`<button class="icon-button" data-action="tray-resolve-all" data-resolution="revert" aria-label="Rechazar todos los cambios">${icon('close')}</button><button class="icon-button" data-action="tray-resolve-all" data-resolution="keep" aria-label="Aceptar todos los cambios">${icon('check')}</button>`;
  items.push(trayRow('diff',`${state.diffs.length} ${state.diffs.length===1?'archivo modificado':'archivos modificados'}`,'file',`<span class="positive">+${added}</span> <span class="negative">−${removed}</span>`,bulk,`${state.diffs.map(({file,added,removed})=>`<div class="tray-file-row"><button class="tray-file" data-action="tray-file" data-file="${file}" aria-label="Revisar ${file}">${icon('file')}<span>${file}</span><span class="positive">+${added}</span><span class="negative">−${removed}</span></button><button class="icon-button tray-hover-action" data-action="tray-resolve" data-file="${file}" data-resolution="revert" aria-label="Rechazar cambios de ${file}">${icon('close')}</button><button class="icon-button tray-hover-action" data-action="tray-resolve" data-file="${file}" data-resolution="keep" aria-label="Aceptar cambios de ${file}">${icon('check')}</button></div>`).join('')}<small>Aceptar conserva los cambios. Rechazar los revierte. Datos de ejemplo.</small>`));
 }

 return items.length?`<div class="composer-trays" aria-label="Actividad sobre el compositor">${items.join('')}</div>`:'';
}
function trayTooltips(){document.querySelectorAll('.composer-trays button').forEach(button=>{button.dataset.tip=button.getAttribute('aria-label')||button.textContent.trim();});}
function refreshTrays(focusId){document.querySelector('[data-action=tray-review-busy]')?.setAttribute('aria-pressed',state.reviewBusy);document.querySelectorAll('.composer-trays-slot').forEach(slot=>{
 if(focusId){slot.querySelectorAll('.composer-tray').forEach(row=>{const expanded=row.dataset.trayKind===state.trayOpen;row.querySelector('.tray-disclosure').setAttribute('aria-expanded',expanded);const detail=row.querySelector('.tray-reveal');detail.inert=!expanded;detail.classList.toggle('is-open',expanded);});slot.querySelector(`[data-tray="${focusId}"]`)?.focus({preventScroll:true});}
 else slot.innerHTML=composerTrays();
});trayTooltips();}
function handleTray(action){
 if(action==='edit'){const input=$('.composer textarea'),draft=input?.value||'';state.draft=state.queued;state.queued=draft;refreshTrays();if(input){input.value=state.draft;input.focus();}if(draft)toast('Tu borrador se conservó en la cola de ejemplo.');return;}
 if(action==='remove'||action==='steer'){state.queued='';refreshTrays();toast(action==='steer'?'Ejecución de ejemplo interrumpida para enviar el mensaje ahora.':'Mensaje de ejemplo quitado de la cola.');return;}
 if(action==='output'||action==='diff'){state.trayOpen=action==='output'?'terminal':'diff';refreshTrays(state.trayOpen);return;}
 dialog(action==='side'?'Abrir en otra conversación':action==='terminal'?'Terminal en segundo plano':'Revisar cambios','Acción ilustrativa. En el IDE abre el recurso real conservando la tarea y sus permisos.');
}
function trays(){return `<div class="lab"><div class="lab-content tray-lab"><h1>Actividad junto al mensaje</h1><p class="intro">Una bandeja integrada para mensajes en cola, terminales y cambios. El compositor queda siempre al frente.</p><div class="tray-demo-toolbar" role="group" aria-label="Ejemplos de bandejas">${[['all','Todo'],['queue','Cola'],['terminal','Terminal'],['diff','Cambios']].map(([id,label])=>`<button class="btn ghost" data-action="tray-filter" data-filter="${id}" aria-pressed="${state.trayFilter===id}">${label}</button>`).join('')}<button class="btn ghost" data-action="tray-review-busy" aria-pressed="${state.reviewBusy}">Simular agente activo</button><button class="btn ghost" data-action="tray-reset">Restablecer ejemplos</button></div>${composer()}<p class="test-note">Desplegá las filas y abrí sus menús. Editar un mensaje lo devuelve al compositor. Datos y acciones de demostración.</p></div></div>`;}

function composer(compact=false){return `<div class="composer-stack"><div class="composer-trays-slot">${composerTrays()}</div><div class="composer">${!compact&&state.attachments?`<div class="attachments">${Array.from({length:state.attachments},(_,i)=>`<div class="attachment"><div class="attachment-preview"><b></b><i></i><i></i><i></i><i></i>Referencia ${i+1}</div><button aria-label="Quitar adjunto ${i+1}" data-action="remove-attachment">${icon('close')}</button></div>`).join('')}</div>`:''}<textarea aria-label="Mensaje" placeholder="${state.plan?'Describe el plan…':'Pregunta o empieza a crear…'}" rows="2"></textarea><div class="composer-actions"><button class="icon-button" data-pop="add" aria-label="Añadir al mensaje" aria-haspopup="menu" aria-expanded="false">${icon('plus')}</button>${pop(icon('shield')+`<span data-permission-label>${state.permission?'Aprobar por mí':'Pedir aprobación'}</span>`,'permissions','btn ghost')}<div class="end">${modelSelector()}${ib('mic','Entrada por voz','demo')}<button class="icon-button send" aria-label="Enviar mensaje de prueba" data-action="send">${icon('arrow')}</button></div></div></div></div>`;}
function chat(){return `<header class="chat-header">${icon('folder')}<span>Migración visual del IDE</span>${ib('more','Opciones de conversación','demo')}<div class="end"><button class="btn ghost" data-action="demo">${icon('external')}Compartir</button>${ib('panel','Mostrar u ocultar contexto','context')}</div></header><div class="chat-layout"><section class="conversation" aria-label="Conversación"><div class="transcript"><div class="user-message">Quiero que toda la interfaz tenga este mismo lenguaje visual.</div><div class="work-summary">Revisado durante 2 min 18 s ${icon('chevron')}</div><p>La propuesta mantiene una estructura tranquila: navegación oscura, contenido abierto y controles que aparecen cuando los necesitás.</p><p>Podés revisar los ajustes, abrir los menús del compositor y probar el comportamiento de los componentes.</p>${activities()}${planPreview()}<details class="tool-card"><summary>${icon('terminal')}Comprobación de componentes ${icon('down')}</summary><pre>✓ Superficies y bordes\n✓ Navegación por teclado\n✓ Menús y controles interactivos\nSalida de ejemplo para revisar el diseño.</pre></details>${state.messages.map(m=>`<div class="user-message">${esc(m)}</div><p>Mensaje recibido en el prototipo. Esta demostración no conecta con un modelo.</p>`).join('')}<div class="answer-tools">${ib('copy','Copiar respuesta','copy')}${ib('refresh','Reintentar','demo')}${ib('more','Más opciones','demo')}</div></div>${composer()}</section>${state.context?`<aside class="context-panel" aria-label="Contexto de la tarea"><div class="context-card"><header>Entorno${ib('plus','Añadir contexto','demo')}</header><div class="context-row">${icon('file')}Cambios<span class="end"><span class="positive">+128</span> <span class="negative">−24</span></span></div><button class="context-row nav-row" data-action="demo">${icon('panel')}Local<span class="end">${icon('down')}</span></button><button class="context-row nav-row" data-action="demo">${icon('git')}main<span class="end">${icon('down')}</span></button><button class="context-row nav-row" data-action="dialog">${icon('git')}Commit o push</button><button class="context-row nav-row" data-action="demo">${icon('external')}Comparar rama</button><div class="separator"></div><header>Fuentes${ib('plus','Añadir fuente','demo')}</header><div class="context-row source">${icon('file')}design.md</div><div class="context-row source">${icon('file')}Referencia visual de ajustes</div><p class="test-note">Datos ilustrativos</p></div></aside>`:''}</div>`;}
const spec=(title,meta,body)=>`<section class="spec-section"><header><h2>${title}</h2><span class="meta">${meta}</span></header>${body}</section>`;
function components(){return `<div class="lab"><div class="lab-content"><h1>Un mismo lenguaje visual</h1><p class="intro">Superficies, controles y estados para aprobar la migración de OpenIDE. Abrí los menús, cambiá los valores y navegá con Tab. Todo funciona solo dentro de esta demostración.</p>${spec('Superficies','Colores medidos en tus capturas',`<div class="swatches">${[['Lateral','#141414'],['Contenido','#181818'],['Grupo','#232323'],['Control','#2a2a2a'],['Popover','#2d2d2d'],['Hover','#3d3d3d']].map(([n,c])=>`<div><div class="swatch" style="background:${c}"></div><div class="swatch-label">${n}<code>${c}</code></div></div>`).join('')}</div>`)}${spec('Acciones','Transparente en reposo · hover local',`<div class="samples"><button class="btn primary" data-action="dialog">Continuar</button><button class="btn" data-action="demo">Cambiar</button><button class="btn outline" data-action="demo">Ver cambios</button><button class="btn ghost" data-action="demo">Más tarde</button><button class="btn danger" data-action="delete">Eliminar</button><button class="btn" disabled>No disponible</button>${ib(state.paused?'play':'pause',state.paused?'Reanudar subagente':'Pausar subagente','pause')}${ib('copy','Copiar','copy')}<span class="badge">Tab para ver foco</span></div>`)}${spec('Controles','Selección, validación y deshabilitado',`<div class="lab-columns"><div class="settings-group">${row('Notificaciones','Recibir avisos cuando termina una tarea.',switcher('notifications','Notificaciones',true))}${row('Modo concentración','Ocultar información secundaria.',switcher('focus','Modo concentración'))}</div><div><label for="sample-name">Nombre del proyecto</label><input id="sample-name" class="field" value="OpenIDE" required aria-describedby="name-help"><p id="name-help" class="test-note">Borrá el nombre para ver la validación.</p><div class="samples" style="margin-top:16px"><label class="check-label"><input type="checkbox" checked>Recordar elección</label><label class="check-label"><input type="radio" name="scope" checked>Proyecto</label><label class="check-label"><input type="radio" name="scope">Global</label></div></div></div>`)}${spec('Estados complementarios','Lectura, ejecución y recuperación',`<div class="samples"><label>Solo lectura<input class="field" readonly value="Proyecto vinculado" aria-label="Proyecto vinculado, solo lectura"></label><div class="sample-surface"><div class="status-line"><span class="spinner" aria-hidden="true"></span>Comprobando cambios…</div><p>Estado de carga ilustrativo</p></div><div class="sample-surface"><h3 style="color:var(--oi-danger)">No se pudo conectar</h3><p>Conservamos tu mensaje para reintentar.</p><button class="btn" style="margin-top:10px" data-action="demo">Reintentar</button></div></div>`)}${spec('Menús y popovers','Escape cierra · flechas recorren opciones',`<div class="samples">${pop('Añadir','add')}${pop('Permisos','permissions')}${pop('Modelo','model')}${modelSelector()}<button class="btn" data-action="dialog">Abrir diálogo</button><button class="btn" data-action="toast">Mostrar aviso</button>${ib('more','Este es un tooltip. También aparece con foco de teclado.','demo')}</div>`)}${spec('Plan y acción principal','Build ámbar de OpenIDE · bordes compartidos',planPreview())}${spec('Actividad de agentes','Adaptación de OpenIDE',`<div style="max-width:650px">${activities()}<details class="tool-card"><summary>${icon('terminal')}Terminal en segundo plano ${icon('down')}</summary><pre>$ npm run dev\nServidor listo en http://localhost:3000\nEsperando cambios…</pre></details></div>`)}${spec('Compositor','24 px · superficie continua',composer())}${spec('Navegación y novedades','Hover, selección y acciones secundarias',`<div class="lab-columns"><div><button class="nav-row active" data-action="task">${icon('folder')}Proyecto seleccionado</button><button class="nav-row task" data-action="task" data-tip="Revisión de herramientas · Proyecto OpenIDE"><span>Revisión de herramientas</span><span class="row-actions" aria-hidden="true">${icon('pin')}${icon('archive')}</span></button></div><div class="sample-surface"><div class="status-line">${icon('cube')}Disponible en OpenIDE</div><h3 style="font-size:18px;margin-top:10px">Un espacio para crear</h3><p>Diseñá, revisá y conectá tus ideas en Canvas.</p><div class="samples" style="margin-top:18px"><button class="btn primary" data-action="demo">Abrir Canvas</button><button class="btn ghost" data-action="dismiss-card">Más tarde</button></div></div></div>`)}<p class="test-note">Geometría estimada a partir de capturas. Editor, GOAL y tarjeta de novedades son adaptaciones propuestas. La especificación completa está en design.md.</p></div></div>`;}
function editor(){return `<div class="editor-layout"><section class="editor-pane"><nav class="tabs" aria-label="Archivos abiertos">${['app.ts','design.md'].map(t=>`<button class="tab ${state.tab===t?'active':''}" data-tab="${t}">${icon('file')}${t}</button>`).join('')}<div style="margin-left:auto">${ib('terminal','Mostrar u ocultar terminal','toggle-terminal')}</div></nav><div class="breadcrumbs">openide / ${state.tab==='app.ts'?'src / app.ts':'design.md'}</div><pre class="code">${(state.tab==='app.ts'?['import { createWorkspace } from "./workspace";','','const workspace = createWorkspace({','  name: "OpenIDE",','  appearance: "neutral",','  panels: ["editor", "chat", "terminal"],','});','','export function start() {','  workspace.open();','}']:['# Diseño de OpenIDE','','Una base neutra. Una estructura consistente.','','## Superficies','','- Navegación: #141414','- Contenido: #181818','- Grupos: #232323','','## Estado','','Dirección visual aprobada. Migración pendiente.']).map((line,i)=>`<span class="number">${i+1}</span>${esc(line)}`).join('\n')}</pre><section class="terminal"><header><span>Terminal</span><span>Problemas</span><span>Salida</span></header><pre>~/projects/openide $ npm run dev\n<span class="positive">✓</span> Servidor listo en http://localhost:3000\n\n<span style="color:#777">Salida ilustrativa · no se ejecutan comandos</span></pre></section></section><aside class="editor-chat"><header class="samples">${icon('chat')}Nueva conversación</header><p style="color:var(--oi-secondary)">La misma interfaz, junto a tu código.</p>${activities()}<p class="test-note">Adaptación propuesta para el editor. La referencia no muestra esta vista.</p>${composer(true)}</aside></div><footer class="editor-status">${icon('git')}main <span>0 errores</span><span class="end">UTF-8 · TypeScript · OpenIDE</span></footer>`;}
function motion(){return `<div class="lab"><div class="lab-content"><h1>Movimiento con intención</h1><p class="intro">Shimmer mientras trabaja. Una transición breve al cambiar de tarea. El mismo ritmo al abrir menús, tooltips y desplegables.</p>${spec('Actividad del agente','Abrí el detalle y reproducí la secuencia',activities())}${spec('Selector de modelo','Código del ZIP · snap, partículas y cambio de vista',`<div class="samples">${modelSelector()}<small>Abrí Avanzado y arrastrá el slider. Ultra muestra el efecto del original.</small></div>`)}${spec('Popovers y tooltip','Densidad compacta de OpenIDE',`<div class="samples">${pop('Añadir','add')}${pop('Permisos','permissions')}<button class="btn outline" data-action="dialog">Abrir diálogo</button>${ib('more','Tooltip con entrada y salida suaves','demo')}</div>`)}${spec('Plan y Build','Ámbar original, borde y separación',planPreview())}<p class="test-note">Todo es una demostración local. El movimiento se reduce automáticamente si lo pide el sistema.</p></div></div>`;}
function render(){if($('.composer textarea'))state.draft=$('.composer textarea').value;closePopover(false);$('#sidebar').innerHTML=sidebar();$('#main').innerHTML=({settings,chat,components,editor,motion,trays})[state.view]();document.querySelectorAll('.views button').forEach(b=>b.setAttribute('aria-current',b.dataset.view===state.view?'page':'false'));$('#shell').style.gridTemplateColumns=state.sidebar?'':'minmax(0,1fr)';$('#sidebar').style.display=state.sidebar?'':'none';if($('.composer textarea'))$('.composer textarea').value=state.draft;trayTooltips();}
let anchor=null,toastTimer,modalAnchor;
function toast(message){$('#toast').textContent=message;$('#toast').hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').hidden=true,3000);}
function closePopover(restore=true){if(!anchor)return;const trigger=anchor;anchor=null;trigger.setAttribute('aria-expanded','false');if(restore)trigger.focus({preventScroll:true});OIMotion.exit($('#popover'));}
function menuRow(label,action,value,i='check',selected=false,desc=''){return `<button role="menuitem${['permission','model','value'].includes(action)?'radio':''}" ${['permission','model','value'].includes(action)?`aria-checked="${selected}"`:''} class="menu-row ${desc?'rich':''}" data-menu-action="${action}" data-value="${esc(value)}">${icon(i)}<span>${desc?`<strong>${label}</strong><small>${desc}</small>`:label}</span>${selected?`<span class="check">${icon('check')}</span>`:''}</button>`;}
function openPopover(button,{keyboard=false}={}){
 const name=button.dataset.pop;if(anchor===button){closePopover();return;}closePopover(false);anchor=button;button.setAttribute('aria-expanded','true');const p=$('#popover');p.className='popover '+name;p.style.width='';OIMotion.cancel(p);p.hidden=false;p.inert=false;p.setAttribute('role',name==='effort'?'dialog':'menu');p.setAttribute('aria-label',({add:'Añadir al mensaje',permissions:'Permisos',model:'Modelo',effort:'Esfuerzo'})[name]||'Seleccionar opción');
 if(name==='permissions')p.innerHTML=`<div class="popover-title">¿Cómo se aprueban las acciones?</div>${menuRow('Pedir aprobación','permission','0','hand',!state.permission,'Pedir permiso para acceder fuera del proyecto.')}${menuRow('Aprobar por mí','permission','1','shield',!!state.permission,'Consultar solo ante acciones potencialmente inseguras.')}`;
 else if(name==='add')p.innerHTML=`<div class="popover-title">Añadir</div>${menuRow('Archivos y carpetas','attach','file','attach')}${menuRow('Goal <span class="detail">Mantener un objetivo activo</span>','goal','goal','goal')}${menuRow('Modo plan <span class="detail">Preparar los próximos pasos</span>','plan','plan','bulb')}<div class="menu-group-label">Herramientas</div>${[['Canvas','Diseños y prototipos','cube'],['Memoria','Decisiones del proyecto','file'],['Navegador','Explorar y comprobar','globe'],['Terminal','Ejecutar y observar','terminal']].map(([l,d,i])=>menuRow(`${l} <span class="detail">${d}</span>`,'tool',l,i)).join('')}`;
 else if(name==='tray-queue')p.innerHTML=`<div class="popover-title">Mensaje en cola</div>${menuRow('Editar mensaje','tray','edit','edit')}${menuRow('Abrir en otra conversación','tray','side','chat')}${menuRow('Quitar de la cola','tray','remove','trash')}`;
 else if(name==='tray-terminal')p.innerHTML=`<div class="popover-title">Terminal</div>${menuRow('Ver salida','tray','output','terminal')}${menuRow('Abrir terminal','tray','terminal','external')}`;
 else if(name==='tray-diff')p.innerHTML=`<div class="popover-title">Cambios de archivos</div>${menuRow('Revisar cambios','tray','diff','file')}${menuRow('Abrir vista de cambios','tray','review','external')}`;
 else if(name==='build')p.innerHTML=`<div class="popover-title">Build</div>${menuRow('Ejecutar el plan','build','run','play')}${menuRow('Revisar antes de ejecutar','build','review','file')}`;

 else {const options=({model:['GPT-6 Astra','Claude','Modelo local'],destination:['OpenIDE','Ventana nueva'],language:['Detección automática','Español','English'],speed:['Estándar','Rápida'],theme:['Oscuro · referencia'],density:['Cómoda','Compacta'],scope:['Proyecto actual','Todos los proyectos']})[name]||['Predeterminado'];p.innerHTML=`<div class="popover-title">${name==='model'?'Modelo · muestra visual':'Seleccionar opción'}</div>${options.map(l=>menuRow(l,name==='model'?'model':'value',l,name==='model'?'cube':'check',name==='model'?state.model===l:button.textContent.trim()===l)).join('')}`;}
 const scroll=document.createElement('div');scroll.className='oi-scroll-body';scroll.append(...p.childNodes);p.append(scroll);
 const r=button.getBoundingClientRect(),placement=name==='add'&&button.closest('.composer')?button.closest('.composer').getBoundingClientRect():r;
 p.style.maxHeight='min(480px,75vh)';
 const position=OIOverlay.layout(placement,{width:p.offsetWidth,height:p.offsetHeight},{width:innerWidth,height:innerHeight});
 if(!position){closePopover(false);return;}
 p.dataset.anchorTop=r.top;p.dataset.anchorLeft=r.left;p.dataset.side=position.side;
 p.style.maxHeight=position.height+'px';p.style.left=position.left+'px';p.style.top=position.top+'px';p.style.transformOrigin=position.side==='above'?'center bottom':'center top';
 OIMotion.enter(p);if(keyboard)p.querySelector('button,input')?.focus({preventScroll:true});

}

function dialog(title,body){modalAnchor=document.activeElement;$('#modal-title').textContent=title;$('#modal-body').textContent=body;$('#modal').setAttribute('aria-labelledby','modal-title');$('#modal').setAttribute('aria-describedby','modal-body');$('#modal').hidden=false;$('#modal').showModal();OIMotion.enter($('#modal'),'dialog');}
document.addEventListener('click',e=>{
 const b=e.target.closest('button');if(!b){if(!e.target.closest('#popover'))closePopover(false);return;}
 if(b.dataset.pop){openPopover(b,{keyboard:e.detail===0});return;}
 if(b.dataset.menuAction){const {menuAction:a,value:v}=b.dataset,previous=anchor;closePopover();if(a==='tray'){handleTray(v);return;}else if(a==='permission'){state.permission=+v;document.querySelectorAll('[data-permission-label]').forEach(n=>n.textContent=state.permission?'Aprobar por mí':'Pedir aprobación');toast('Permiso de muestra actualizado.');}else if(a==='model'){state.model=v;document.querySelectorAll('[data-model-label]').forEach(n=>n.textContent=v);toast('Modelo de muestra: '+v);}else if(a==='value'){previous.innerHTML=esc(v)+icon('down');toast('Valor de muestra: '+v);}else if(a==='build'){if(v==='run')startActivity();else dialog('Revisar plan','Revisión visual del plan antes de ejecutar. No modifica el IDE.');}else if(a==='attach'){state.attachments=Math.min(4,state.attachments+1);render();toast('Referencia de ejemplo añadida.');}else if(a==='plan'){state.plan=!state.plan;document.querySelectorAll('.composer textarea').forEach(n=>n.placeholder=state.plan?'Describe el plan…':'Pregunta o empieza a crear…');toast(state.plan?'Modo plan activado en la muestra.':'Modo plan desactivado.');}else if(a==='goal')dialog('Nuevo objetivo','Así se presenta una acción que necesita más contexto. En el IDE se abrirá el flujo real de GOAL.');else toast(v+' · acción ilustrativa.');return;}
 if(!e.target.closest('#popover'))closePopover(false);
 if(b.dataset.view){state.view=b.dataset.view;render();return;}if(b.dataset.category){state.category=b.dataset.category;render();return;}if(b.dataset.tab){state.tab=b.dataset.tab;render();return;}
 if(b.dataset.switch){const checked=b.getAttribute('aria-checked')!=='true';state.switches[b.dataset.switch]=checked;b.setAttribute('aria-checked',checked);return;}
 switch(b.dataset.action){
 case 'tray-filter':state.trayFilter=b.dataset.filter;state.trayOpen=null;document.querySelectorAll('[data-action=tray-filter]').forEach(n=>n.setAttribute('aria-pressed',n.dataset.filter===state.trayFilter));refreshTrays();break;
 case 'tray-toggle':state.trayOpen=state.trayOpen===b.dataset.tray?null:b.dataset.tray;refreshTrays(b.dataset.tray);break;
 case 'tray-remove':handleTray('remove');break;
 case 'tray-steer':handleTray('steer');break;
 case 'tray-stop':state.terminalStopped=!state.terminalStopped;refreshTrays();toast(state.terminalStopped?'Terminal de ejemplo detenida.':'Terminal de ejemplo iniciada.');break;
 case 'tray-reset':state.diffs=sampleDiffs();state.reviewBusy=false;state.queued='Ajustá los bordes y verificá los popovers';state.terminalStopped=false;state.trayOpen=null;refreshTrays();break;
 case 'tray-review-busy':state.reviewBusy=!state.reviewBusy;b.setAttribute('aria-pressed',state.reviewBusy);refreshTrays();break;
 case 'tray-review-stop':state.reviewBusy=false;document.querySelector('[data-action=tray-review-busy]')?.setAttribute('aria-pressed','false');refreshTrays();toast('Agente de ejemplo detenido. Ya podés revisar el conjunto.');break;
 case 'tray-resolve':case 'tray-resolve-all':{if(b.dataset.action==='tray-resolve-all'&&state.reviewBusy)return;const keep=b.dataset.resolution==='keep';state.diffs=b.dataset.action==='tray-resolve-all'?[]:state.diffs.filter(f=>f.file!==b.dataset.file);refreshTrays();document.querySelector('[data-tray=diff]')?.focus({preventScroll:true});toast(keep?'Cambios de ejemplo aceptados.':'Cambios de ejemplo rechazados.');break;}
 case 'tray-file':dialog(b.dataset.file,'Vista de diff ilustrativa. En producción abre el editor de diferencias del IDE.');break;
 case 'sidebar':state.sidebar=!state.sidebar;render();break;
 case 'settings':state.view='settings';render();break;
 case 'new':state.view='chat';state.messages=[];state.draft='';if($('.composer textarea'))$('.composer textarea').value='';render();toast('Conversación de prueba.');break;
 case 'task':state.view='chat';render();break;
 case 'context':state.context=!state.context;render();break;
 case 'remove-attachment':state.attachments--;b.closest('.attachment').remove();if(!state.attachments)b.closest('.attachments')?.remove();break;
 case 'pause':state.paused=!state.paused;paintActivity();break;
 case 'activity-file':dialog(b.dataset.file,'Lectura de ejemplo utilizada para revisar estilos, animaciones y componentes en esta maqueta.');break;
 case 'activity-details':state.activityOpen=!state.activityOpen;document.querySelectorAll('[data-action="activity-details"]').forEach(n=>n.setAttribute('aria-expanded',state.activityOpen));document.querySelectorAll('.activity-reveal').forEach(n=>{n.inert=!state.activityOpen;n.classList.toggle('is-open',state.activityOpen);});break;
 case 'replay-activity':case 'build-demo':startActivity();break;
 case 'next-activity':clearInterval(activityTimer);state.demoRunning=false;state.activityStep=(state.activityStep+1)%5;paintActivity();break;
 case 'send':{const input=b.closest('.composer').querySelector('textarea'),value=input.value.trim();if(!value){input.focus();return;}state.messages.push(value);input.value='';state.draft='';if(state.view==='chat'){render();$('.transcript').scrollTop=$('.transcript').scrollHeight;}else{input.value='';toast('Mensaje de prueba recibido.');}break;}
 case 'folder':dialog('Cambiar carpeta','En el IDE se abrirá el selector del sistema. Esta vista permite revisar el diálogo sin cambiar archivos.');break;
 case 'delete':dialog('Eliminar elemento','Este diálogo solo representa el estilo de una confirmación. No se eliminará ningún archivo.');break;
 case 'goal':dialog('Unificar el diseño del IDE','3 de 5 hitos de ejemplo. Informe, cambios y validaciones comparten el mismo lenguaje visual.');break;
 case 'terminal':dialog('Terminal en segundo plano','Servidor listo. Este ejemplo permite revisar la interfaz sin ejecutar procesos.');break;
 case 'toggle-terminal':$('.terminal').hidden=!$('.terminal').hidden;break;
 case 'dialog':dialog('Revisar acción','Confirmación de ejemplo. Las acciones del prototipo no afectan al IDE ni a tus proyectos.');break;
 case 'confirm-dialog':closeDialog();toast('Acción confirmada en la demostración.');break;
 case 'cancel-dialog':closeDialog();break;
 case 'dismiss-card':b.closest('.sample-surface').remove();toast('Novedad descartada en la muestra.');break;
 case 'copy':toast('Acción de copiar ilustrativa.');break;
 default:toast('Vista de prueba · no modifica el IDE.');
 }
});
document.addEventListener('input',e=>{const t=e.target;if(t.id==='sample-name'){const invalid=!t.value.trim();t.classList.toggle('invalid',invalid);t.setAttribute('aria-invalid',invalid);$('#name-help').textContent=invalid?'Ingresá un nombre para el proyecto.':'Borrá el nombre para ver la validación.';$('#name-help').className=invalid?'field-help':'test-note';}if(t.id==='settings-search'){const query=t.value.toLowerCase().trim();document.querySelectorAll('.setting').forEach(r=>r.hidden=!r.dataset.search.includes(query));document.querySelectorAll('.settings-section').forEach(s=>s.hidden=![...s.querySelectorAll('.setting')].some(r=>!r.hidden));$('#search-empty').hidden=[...document.querySelectorAll('.setting')].some(r=>!r.hidden);}});
document.addEventListener('keydown',e=>{if(e.key==='ArrowDown'&&e.target.matches('[data-pop]')){e.preventDefault();if(anchor!==e.target)openPopover(e.target,{keyboard:true});else $('#popover').querySelector('button,input')?.focus({preventScroll:true});return;}if(e.key==='Escape'){closePopover();hideTip();}if((e.ctrlKey||e.metaKey)&&e.key===','){e.preventDefault();state.view='settings';render();}if(!$('#popover').hidden&&e.target.closest('#popover')&&!e.target.matches('input')&&['ArrowDown','ArrowUp','Home','End'].includes(e.key)){e.preventDefault();const items=[...$('#popover').querySelectorAll('button')],i=items.indexOf(document.activeElement);items[e.key==='Home'?0:e.key==='End'?items.length-1:(i+(e.key==='ArrowDown'?1:-1)+items.length)%items.length]?.focus();}if(e.key==='Tab'&&!$('#popover').hidden)setTimeout(()=>{if(!$('#popover').contains(document.activeElement))closePopover(false);},0);});
function closeDialog(){OIMotion.exit($('#modal'),'dialog',()=>{$('#modal').close();$('#modal').hidden=false;});}
$('#modal').addEventListener('cancel',e=>{e.preventDefault();closeDialog();});
$('#modal').addEventListener('close',()=>modalAnchor?.focus());
window.addEventListener('resize',()=>{closePopover(false);hideTip();});document.addEventListener('scroll',e=>{if(anchor&&!$('#popover').contains(e.target)){const r=anchor.getBoundingClientRect();if(r.top!==Number($('#popover').dataset.anchorTop)||r.left!==Number($('#popover').dataset.anchorLeft))closePopover(false);}hideTip();},true);
document.addEventListener('change',e=>{if(e.target.matches('openide-model-selector')&&Number.isInteger(e.detail?.index)){state.effort=e.detail.index;document.querySelectorAll('openide-model-selector').forEach(n=>{if(n!==e.target)n.value=state.effort;});}});
document.addEventListener('modelchange',e=>{state.model=e.detail;document.querySelectorAll('openide-model-selector').forEach(n=>n.setAttribute('model-name',state.model));});
render();
