# OpenIDE keyboard shortcuts

OpenIDE keeps the command map compatible with Code OSS and with the extension
ecosystem. Browse and customize every shortcut from **File > Preferences >
Keyboard Shortcuts** (`Ctrl+K Ctrl+S` on Linux and Windows, `⌘K ⌘S` on macOS).
